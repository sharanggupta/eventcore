/**
 * Webhook subscription lifecycle: register (secret shown once), list, delete, and
 * update per-subscription event-type filters in place. Actual delivery lives in
 * {@link dev.eventcore.deliveries}.
 */
package dev.eventcore.webhooks;
