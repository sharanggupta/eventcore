/**
 * Small cryptographic utilities: SHA-256 hashing (API keys at rest), HMAC-SHA256
 * (webhook delivery signatures), and random secret generation with recognizable prefixes.
 */
package dev.eventcore.crypto;
